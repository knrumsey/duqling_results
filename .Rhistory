boxplots_sim_study(tmp, metric="COVER0.95")
tmp <- filter_sim_study(duq_testfuncs,
id="dms_additive",
n_train=1000, NSR=0.1)
boxplots_sim_study(tmp, metric="COVER0.95")
tmp_loss <- mutate_sim_study(tmp,
asym_loss_95 = ifelse(COVER0.95 < 0.95,
(0.95 - COVER0.95) * 2,
(COVER0.95 - 0.95)))
remove.packages("duqling")
devtools::install_github("knrumsey/duqling")
tmp <- filter_sim_study(duq_testfuncs,
id="dms_additive",
n_train=1000, NSR=0.1)
#| echo: true
#| eval: true
# Install and load duqling
# devtools::install_github("knrumsey/duqling")
library(duqling)
funcs <- get_emulator_functions("bass")
fit_bass  <- funcs$fit_func
pred_bass <- funcs$pred_func
# Sim study on the 60 test functions
data("sim_study_testfuncs")
duq_testfuncs <- process_sim_study(sim_study_testfuncs)
# Sim study on the 40 test functions
data("sim_study_realdata")
duq_realdata <- process_sim_study(sim_study_realdata)
tmp <- filter_sim_study(duq_testfuncs,
id="dms_additive",
n_train=1000, NSR=0.1)
boxplots_sim_study(tmp, metric="COVER0.95")
tmp_loss <- mutate_sim_study(tmp,
asym_loss_95 = ifelse(COVER0.95 < 0.95,
(0.95 - COVER0.95) * 2,
(COVER0.95 - 0.95)))
boxplots_sim_study(duq_tmp, metric = "asym_loss_95",
title = "Asymmetric Loss (0.95 Interval)")
tmp_loss <- mutate_sim_study(tmp,
asym_loss_95 = ifelse(COVER0.95 < 0.95,
(0.95 - COVER0.95) * 2,
(COVER0.95 - 0.95)))
boxplots_sim_study(tmp_loss, metric = "asym_loss_95",
title = "Asymmetric Loss (0.95 Interval)")
tmp <- filter_sim_study(duq_testfuncs,
id="borehole",
n_train=1000, NSR=0.1)
boxplots_sim_study(tmp, metric="COVER0.95")
tmp_loss <- mutate_sim_study(tmp,
asym_loss_95 = ifelse(COVER0.95 < 0.95,
(0.95 - COVER0.95) * 2,
(COVER0.95 - 0.95)))
boxplots_sim_study(tmp_loss, metric = "asym_loss_95",
title = "Asymmetric Loss (0.95 Interval)")
tmp_loss <- mutate_sim_study(tmp,
asym_loss_95 = ifelse(COVER0.95 < 0.95,
(0.95 - COVER0.95),
(COVER0.95 - 0.95)*2
))
boxplots_sim_study(tmp_loss, metric = "asym_loss_95",
title = "Asymmetric Loss (0.95 Interval)")
duq <- process_sim_study(sim_study_testfuncs)
names(duq$df)
?run_sim_study
logit
tmp_IAE <- mutate_sim_study(tmp,
IAE_est = 0.25 * abs(COVER0.8 - 0.8) +
0.25 * abs(COVER0.9 - 0.9) +
0.25 * abs(COVER0.95 - 0.95) +
0.25 * abs(COVER0.99 - 0.99))
boxplots_sim_study(tmp_IAE, metric = "IAE_est",
title = "Estimated IAE (Intervals)")
tmp <- filter_sim_study(duq_testfuncs,
id="borehole",
n_train=1000, NSR=0.1)
boxplots_sim_study(tmp, metric="COVER0.95")
duq1 <- process_sim_study(sim_study_realdata)
duq2 <- process_sim_study(sim_study_testfuncs)
duq12 <- join_sim_study(duq1, duq2)
summarize_sim_study(duq12)
?summarize_sim_study
summarize_sim_study
duq_total <- join_sim_study(duq_testfuncs, duq_realdata)
table <- summarize_sim_study(duq_total,
summarize="time", summary_fun = function(val) c(mean=mean(val)),
win_rate  = c("CRPS", "FVU"),
topK_rate = c("CRPS", "FVU"), K=10,
soft_rel  = c("CRPS", "FVU"), epsilon=c(0.001, 0.001))
# Make table with stargazer package
library(stargazer)
stargazer(table, summary = FALSE, type="text")
duq_total <- join_sim_study(duq_testfuncs, duq_realdata)
table <- summarize_sim_study(duq_total,
summarize="time", summary_fun = function(val) c(mean=mean(val)),
win_rate  = c("CRPS", "FVU"),
topK_rate = c("CRPS", "FVU"), K=10,
soft_rel  = c("CRPS", "FVU"), epsilon=c(0.001, 0.001))
# Make table with stargazer package
knitr::kable(table)
duq_total <- join_sim_study(duq_testfuncs, duq_realdata)
table <- summarize_sim_study(duq_total,
summarize="time", summary_fun = function(val) c(mean=mean(val)),
win_rate  = c("CRPS", "FVU"),
topK_rate = c("CRPS", "FVU"), K=10,
soft_rel  = c("CRPS", "FVU"), epsilon=c(0.001, 0.001))
# Make table with stargazer package
class(table)
# Sim study on the 60 test functions
data("sim_study_testfuncs")
duq_testfuncs <- process_sim_study(sim_study_testfuncs)
# Sim study on the 40 test functions
data("sim_study_realdata")
duq_realdata <- process_sim_study(sim_study_realdata)
?stargazer
?kable
#| results: asis
duq_total <- join_sim_study(duq_testfuncs, duq_realdata)
table <- summarize_sim_study(duq_total,
summarize="time", summary_fun = function(val) c(mean=mean(val)),
win_rate  = c("CRPS", "FVU"),
topK_rate = c("CRPS", "FVU"), K=10,
soft_rel  = c("CRPS", "FVU"), epsilon=c(0.001, 0.001))
# Make table
knitr::kable(table, digits=c(NA, rep(3, 7)), align=c("lccccccc"))
#stargazer::stargazer(table, summary=FALSE, type="text")
class(table$CRPS_winrate)
class(table$time_mean)
class(table[,1])
class(table[,3])
class(table[,4])
for(j in 2:ncol(table)){
table[,j] <- as.numeric(table[,j])
}
class(table[,4])
#| results: asis
duq_total <- join_sim_study(duq_testfuncs, duq_realdata)
table <- summarize_sim_study(duq_total,
summarize="time", summary_fun = function(val) c(mean=mean(val)),
win_rate  = c("CRPS", "FVU"),
topK_rate = c("CRPS", "FVU"), K=10,
soft_rel  = c("CRPS", "FVU"), epsilon=c(0.001, 0.001))
#| echo: false
#| eval: true
for(j in 2:ncol(table)){
table[,j] <- as.numeric(table[,j])
}
knitr::kable(table, digits=3, align=c("lccccccc"))
#stargazer::stargazer(table, summary=FALSE, type="text")
class(table$CRPS_softrel)
colnames(tables)
colnames(table)
table$CRPS_winrate  == 0
sum(table$CRPS_winrate  == 0)
32 - sum(table$CRPS_winrate  == 0)
remove.packages("duqling")
devtools::install_github("knrumsey/duqling")
# Ensure CRPS ranks exist
duq_testfuncs <- rank_sim_study(duq_testfuncs, metric = "CRPS")
#| echo: true
#| eval: true
# Install and load duqling
# devtools::install_github("knrumsey/duqling")
library(duqling)
funcs <- get_emulator_functions("bass")
fit_bass  <- funcs$fit_func
pred_bass <- funcs$pred_func
# Sim study on the 60 test functions
data("sim_study_testfuncs")
duq_testfuncs <- process_sim_study(sim_study_testfuncs)
# Sim study on the 40 test functions
data("sim_study_realdata")
duq_realdata <- process_sim_study(sim_study_realdata)
# Ensure CRPS ranks exist
duq_testfuncs <- rank_sim_study(duq_testfuncs, metric = "CRPS")
df <- duq_testfuncs$df
# Keep only unique winners
best_idx <- which(df$CRPS_rank == 1)
best_df <- df[best_idx, , drop = FALSE]
# Count how many winners each id has within each scenario
scenario_counts <- ave(best_df$method, best_df$sim_scenario, FUN = length)
# Keep only scenarios with a unique winner
best_df <- best_df[scenario_counts == 1, , drop = FALSE]
# Count number of wins by method and id
tab <- table(best_df$method, best_df$id)
# Keep only methods with at least one win
tab <- tab[rowSums(tab) > 0, , drop = FALSE]
# Build summary table
methods <- rownames(tab)
n_ids_best <- rowSums(tab > 0)
ids_best <- apply(tab, 1, function(x) {
paste(colnames(tab)[x > 0], collapse = ", ")
})
total_wins <- rowSums(tab)
best_table_testfuncs <- data.frame(
method = methods,
n_functions_best = n_ids_best,
total_wins = total_wins,
functions_best = ids_best,
stringsAsFactors = FALSE
)
best_table_testfuncs <- best_table_testfuncs[order(-best_table_testfuncs$n_functions_best,
-best_table_testfuncs$total_wins,
best_table_testfuncs$method), ]
knitr::kable(best_table_testfuncs, align = c("l", "r", "r", "l"))
# Ensure CRPS ranks exist
duq_testfuncs <- rank_sim_study(duq_testfuncs, metric = "CRPS")
df <- duq_testfuncs$df
# Keep only winners
best_df <- df[df$CRPS_rank == 1, , drop = FALSE]
# Keep only scenarios with a unique winner
scenario_counts <- ave(best_df$method, best_df$sim_scenario, FUN = length)
best_df <- best_df[scenario_counts == 1, , drop = FALSE]
# Build table: one row per function
ids <- sort(unique(best_df$id))
methods_best <- sapply(ids, function(z) {
paste(sort(unique(best_df$method[best_df$id == z])), collapse = ", ")
})
n_methods_best <- sapply(ids, function(z) {
length(unique(best_df$method[best_df$id == z]))
})
total_wins <- sapply(ids, function(z) {
sum(best_df$id == z)
})
best_table_testfuncs <- data.frame(
id = ids,
n_methods_best = n_methods_best,
total_wins = total_wins,
methods_best = methods_best,
stringsAsFactors = FALSE
)
best_table_testfuncs <- best_table_testfuncs[
order(-best_table_testfuncs$n_methods_best,
-best_table_testfuncs$total_wins,
best_table_testfuncs$id),
]
knitr::kable(best_table_testfuncs, align = c("l", "r", "r", "l"))
# Ensure CRPS ranks exist
duq_testfuncs <- rank_sim_study(duq_testfuncs, metric = "CRPS")
df <- duq_testfuncs$df
# Keep only winners
best_df <- df[df$CRPS_rank == 1, , drop = FALSE]
# Keep only scenarios with a unique winner
scenario_counts <- ave(best_df$method, best_df$sim_scenario, FUN = length)
best_df <- best_df[scenario_counts == 1, , drop = FALSE]
# Build table: one row per function
ids <- sort(unique(best_df$id))
methods_best <- sapply(ids, function(z) {
paste(sort(unique(best_df$method[best_df$id == z])), collapse = ", ")
})
n_methods_best <- sapply(ids, function(z) {
length(unique(best_df$method[best_df$id == z]))
})
total_wins <- sapply(ids, function(z) {
sum(best_df$id == z)
})
best_table_testfuncs <- data.frame(
id = ids,
n_methods_best = n_methods_best,
total_wins = total_wins,
methods_best = methods_best,
stringsAsFactors = FALSE
)
best_table_testfuncs <- best_table_testfuncs[
order(-best_table_testfuncs$n_methods_best,
-best_table_testfuncs$total_wins,
best_table_testfuncs$id),
]
rownames(best_table_testfuncs)
colnames(best_table_testfuncs)
# Ensure CRPS ranks exist
duq_testfuncs <- rank_sim_study(duq_testfuncs, metric = "CRPS")
df <- duq_testfuncs$df
# Keep only winners
best_df <- df[df$CRPS_rank == 1, , drop = FALSE]
# Keep only scenarios with a unique winner
scenario_counts <- ave(best_df$method, best_df$sim_scenario, FUN = length)
best_df <- best_df[scenario_counts == 1, , drop = FALSE]
# Build table: one row per function
ids <- sort(unique(best_df$id))
methods_best <- sapply(ids, function(z) {
paste(sort(unique(best_df$method[best_df$id == z])), collapse = ", ")
})
n_methods_best <- sapply(ids, function(z) {
length(unique(best_df$method[best_df$id == z]))
})
total_wins <- sapply(ids, function(z) {
sum(best_df$id == z)
})
best_table_testfuncs <- data.frame(
id = ids,
n_methods_best = n_methods_best,
total_wins = total_wins,
methods_best = methods_best,
stringsAsFactors = FALSE
)
best_table_testfuncs <- best_table_testfuncs[
order(-best_table_testfuncs$n_methods_best,
-best_table_testfuncs$total_wins,
best_table_testfuncs$id),
]
knitr::kable(best_table_testfuncs, align = c("l", "r", "r", "l"))
# Ensure CRPS ranks exist
duq_testfuncs <- rank_sim_study(duq_testfuncs, metric = "CRPS")
df <- duq_testfuncs$df
# Keep only winners
best_df <- df[df$CRPS_rank == 1, , drop = FALSE]
# Keep only scenarios with a unique winner
scenario_counts <- ave(best_df$method, best_df$sim_scenario, FUN = length)
best_df <- best_df[scenario_counts == 1, , drop = FALSE]
# Build table: one row per function
ids <- sort(unique(best_df$id))
methods_best <- sapply(ids, function(z) {
paste(sort(unique(best_df$method[best_df$id == z])), collapse = ", ")
})
n_methods_best <- sapply(ids, function(z) {
length(unique(best_df$method[best_df$id == z]))
})
total_wins <- sapply(ids, function(z) {
sum(best_df$id == z)
})
best_table_testfuncs <- data.frame(
id = ids,
n_methods_best = n_methods_best,
methods_best = methods_best,
stringsAsFactors = FALSE
)
best_table_testfuncs <- best_table_testfuncs[
order(-best_table_testfuncs$n_methods_best,
best_table_testfuncs$id),
]
rownames(best_table_testfuncs) <- NULL
colnames(best_table_testfuncs) <- c("Function", "# of Best", "Best Emulators")
knitr::kable(best_table_testfuncs, align = c("l", "c", "l"))
# Ensure CRPS ranks exist
duq_testfuncs <- rank_sim_study(duq_testfuncs, metric = "CRPS")
df <- duq_testfuncs$df
# Keep only winners
best_df <- df[df$CRPS_rank == 1, , drop = FALSE]
# Keep only scenarios with a unique winner
scenario_counts <- ave(best_df$method, best_df$sim_scenario, FUN = length)
best_df <- best_df[scenario_counts == 1, , drop = FALSE]
# Build table: one row per function
ids <- sort(unique(best_df$id))
methods_best <- sapply(ids, function(z) {
paste(sort(unique(best_df$method[best_df$id == z])), collapse = ", ")
})
n_methods_best <- sapply(ids, function(z) {
length(unique(best_df$method[best_df$id == z]))
})
total_wins <- sapply(ids, function(z) {
sum(best_df$id == z)
})
best_table_testfuncs <- data.frame(
id = ids,
n_methods_best = n_methods_best,
methods_best = methods_best,
stringsAsFactors = FALSE
)
rownames(best_table_testfuncs) <- NULL
colnames(best_table_testfuncs) <- c("Function", "# of Best", "Best Emulators")
knitr::kable(best_table_testfuncs, align = c("l", "c", "l"))
80/12
#| echo: false
#| eval: true
fmt <- function(x, digits = 3) {
ifelse(abs(x) < 1e-3 | abs(x) > 1e4,
formatC(x, format = "e", digits = digits),
formatC(x, format = "f", digits = digits))
}
duq_total <- join_sim_study(duq_testfuncs, duq_realdata)
# ---- CRPS TABLE ----
table_crps <- summarize_sim_study(
duq_total,
summarize = "time", summary_fun = function(val) c(mean = mean(val)),
win_rate  = "CRPS",
topK_rate = "CRPS", K = 10,
soft_rel  = "CRPS", epsilon = 0.001
)
# Convert to numeric
for(j in 2:ncol(table_crps)){
table_crps[,j] <- as.numeric(table_crps[,j])
}
# Format numeric columns
table_crps$time_mean       <- fmt(table_crps$time_mean)
table_crps$CRPS_winrate    <- fmt(table_crps$CRPS_winrate)
table_crps$CRPS_top10rate  <- fmt(table_crps$CRPS_top10rate)
table_crps$CRPS_softrel    <- fmt(table_crps$CRPS_softrel)
# Rename columns
colnames(table_crps) <- c(
"Method",
"Time (mean)",
"CRPS (win rate)",
"CRPS (top 10%)",
"CRPS (soft relative)"
)
knitr::kable(table_crps, align = "lcccc")
# ---- FVU TABLE ----
table_fvu <- summarize_sim_study(
duq_total,
summarize = "time", summary_fun = function(val) c(mean = mean(val)),
win_rate  = "FVU",
topK_rate = "FVU", K = 10,
soft_rel  = "FVU", epsilon = 0.001
)
# Convert to numeric
for(j in 2:ncol(table_fvu)){
table_fvu[,j] <- as.numeric(table_fvu[,j])
}
# Format numeric columns
table_fvu$time_mean      <- fmt(table_fvu$time_mean)
table_fvu$FVU_winrate    <- fmt(table_fvu$FVU_winrate)
table_fvu$FVU_top10rate  <- fmt(table_fvu$FVU_top10rate)
table_fvu$FVU_softrel    <- fmt(table_fvu$FVU_softrel)
# Rename columns
colnames(table_fvu) <- c(
"Method",
"Time (mean)",
"FVU (win rate)",
"FVU (top 10%)",
"FVU (soft relative)"
)
knitr::kable(table_fvu, align = "lcccc")
#| echo: false
#| eval: true
fmt <- function(x, digits = 3) {
ifelse(abs(x) < 1e-3 | abs(x) > 1e4,
formatC(x, format = "e", digits = digits),
formatC(x, format = "f", digits = digits))
}
duq_total <- join_sim_study(duq_testfuncs, duq_realdata)
# ---- CRPS TABLE ----
table_crps <- summarize_sim_study(
duq_total,
summarize = "time", summary_fun = function(val) c(mean = mean(val)),
win_rate  = "CRPS",
topK_rate = "CRPS", K = 10,
soft_rel  = "CRPS", epsilon = 0.001
)
class(table_crps)
dim(table_crps)
#| echo: false
#| eval: true
fmt <- function(x, digits = 3) {
ifelse(abs(x) < 1e-3 | abs(x) > 1e4,
formatC(x, format = "e", digits = digits),
formatC(x, format = "f", digits = digits))
}
duq_total <- join_sim_study(duq_testfuncs, duq_realdata)
# ---- CRPS TABLE ----
table_crps <- summarize_sim_study(
duq_total,
summarize = "time", summary_fun = function(val) c(mean = mean(val)),
win_rate  = "CRPS",
topK_rate = "CRPS", K = 10,
soft_rel  = "CRPS", epsilon = 0.001
)
# Format numeric columns
table_crps$time_mean       <- fmt(table_crps$time_mean)
table_crps$CRPS_winrate    <- fmt(table_crps$CRPS_winrate)
table_crps$CRPS_top10rate  <- fmt(table_crps$CRPS_top10rate)
table_crps$CRPS_softrel    <- fmt(table_crps$CRPS_softrel)
# Rename columns
colnames(table_crps) <- c(
"Method",
"Time (mean)",
"CRPS (win rate)",
"CRPS (top 10%)",
"CRPS (soft relative)"
)
knitr::kable(table_crps, align = "lcccc")
#| echo: false
#| eval: true
fmt <- function(x, digits = 3) {
ifelse(abs(x) < 1e-3 | abs(x) > 1e4,
formatC(x, format = "e", digits = digits),
formatC(x, format = "f", digits = digits))
}
duq_total <- join_sim_study(duq_testfuncs, duq_realdata)
# ---- CRPS TABLE ----
table_crps <- summarize_sim_study(
duq_total,
summarize = "time", summary_fun = function(val) c(mean = mean(val)),
win_rate  = "CRPS",
topK_rate = "CRPS", K = 10,
soft_rel  = "CRPS", epsilon = 0.001
)
knitr::kable(table_crps, align = "lcccc")
#| echo: false
#| eval: true
fmt <- function(x, digits = 3) {
ifelse(abs(x) < 1e-3 | abs(x) > 1e4,
formatC(x, format = "e", digits = digits),
formatC(x, format = "f", digits = digits))
}
duq_total <- join_sim_study(duq_testfuncs, duq_realdata)
# ---- CRPS TABLE ----
table_crps <- summarize_sim_study(
duq_total,
summarize = "time", summary_fun = function(val) c(mean = mean(val)),
win_rate  = "CRPS",
topK_rate = "CRPS", K = 10,
soft_rel  = "CRPS", epsilon = 0.001
)
knitr::kable(table_crps, align = "lcccc")
duq <- process_sim_study(sim_study_testfuncs)
summarize_sim_study(duq)
remove.packages("duqling")
devtools::install_github("knrumsey/duqling")
library(duqling)
remove.packages("duqling")
devtools::install_github("knrumsey/duqling")
1e4
